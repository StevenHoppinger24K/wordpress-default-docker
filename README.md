# WordPress default Docker

A basic WordPress development environment using Docker. 

Run `docker-compose up --build` and go to http://localhost:8000 and you are ready to go with your fresh WordPress instalation. 